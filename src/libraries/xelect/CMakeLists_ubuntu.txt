if (DISABLE_REAL_STAKE)
    add_definitions(-DXENABLE_MOCK_ZEC_STAKE)
endif()

aux_source_directory(./src src_dir)

add_library(xelect STATIC ${src_dir})

add_dependencies(xelect xconfig xvnetwork xdata xvm xstore xrouter xxbase)

find_library(jsoncpp_LIBRARIES jsoncpp HINTS ${LIBRARY_OUTPUT_DIRECTORY} REQUIRED)
target_link_libraries(xelect PRIVATE xsafebox xconfig xvnetwork xdata xvm protobuf xstore xrouter xxbase ${jsoncpp_LIBRARIES} pthread)
