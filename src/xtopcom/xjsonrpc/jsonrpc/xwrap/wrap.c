#include<stdio.h>
#include<wrap.h>

//wrap eth tx
int wrapEthTx(char *rawTx){
    //rawTx -> wrapedTx
    //sendWrappedEthTx(wrapedTx)
    return 0;
}

//send wrapped tx to Auditor network
int sendWrappedEthTx(char *wrapedTx){
    //send wrapped eth tx to Auditor
    return 0;
}
