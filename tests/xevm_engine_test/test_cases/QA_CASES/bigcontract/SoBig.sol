pragma solidity ^0.8.10;

contract Array {
    
    function a(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function b(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function c(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function d(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function e(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function f(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function g(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function h(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function i(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function j(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function k(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function l(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function m(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function n(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function o(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function p(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function q(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function r(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function s(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function t(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function u(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function v(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function w(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function x(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function y(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function z(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function aa(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function bb(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function cc(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function dd(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ee(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ff(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function gg(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function hh(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ii(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function jj(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function kk(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ll(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function mm(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function nn(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function oo(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function pp(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function qq(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function rr(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ss(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function tt(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function uu(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function vv(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    function ww(uint256 x,uint256 y) public returns(uint256){
        uint[] memory a = new uint[](3);
        a[0] = 1;
        return a[x] + a[y];
    }
    
}

